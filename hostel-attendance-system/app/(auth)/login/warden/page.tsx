import { LoginFormCard } from '@/components/auth/login-form'

export default function WardenLoginPage() {
  return (
    <LoginFormCard
      role="admin"
      title="Warden Login"
      description="Sign in to manage hostel operations and attendance records."
    />
  )
}
